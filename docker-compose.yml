services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SQLITE_PATH: /data/wow_db.sqlite
    volumes:
      - kinopub_data:/data

volumes:
  kinopub_data:
